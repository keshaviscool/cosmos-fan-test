name: 'dwh_transforms'
version: '1.0.0'
config-version: 2
profile: 'dwh_transforms'

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dwh_transforms:
    staging:
      +materialized: view
    marts:
      +materialized: table